import classes from "./Theme.module.css";

const Theme = () => {
  return (
    <div className={classes.theme}>
      <label className={classes.label}>
        The Christian Marriage in Practice
      </label>
    </div>
  );
};

export default Theme;
